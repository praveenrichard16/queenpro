{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build && mkdir -p dist && cp -r app bootstrap config database public resources routes storage vendor artisan composer.json composer.lock package.json dist/ && touch dist/.vercel-ready",
        "dev": "vite"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "bootstrap": "^5.2.3",
        "laravel-vite-plugin": "^0.7.2",
        "sass": "^1.56.1",
        "vite": "^4.0.0"
    }
}
