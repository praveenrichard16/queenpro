{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build && mkdir -p dist && rsync -av --exclude='node_modules' --exclude='.git' --exclude='dist' --exclude='.vercel' . dist/ || (mkdir -p dist && cp -r app bootstrap config database public resources routes storage vendor artisan composer.json composer.lock package.json dist/ 2>/dev/null || true)",
        "dev": "vite"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "bootstrap": "^5.2.3",
        "laravel-vite-plugin": "^0.7.2",
        "sass": "^1.56.1",
        "vite": "^4.0.0"
    }
}
